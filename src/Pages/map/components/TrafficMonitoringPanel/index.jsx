import React from "react";

/**
 * TrafficMonitoringPanel component
 * Note: This component's functionality has been moved to ActiveModuleComponents
 * This is kept as a placeholder for backward compatibility
 */
const TrafficMonitoringPanel = ({ map }) => {
  // This component is now a placeholder
  // All module-specific rendering is handled by ActiveModuleComponents
  return null;
};

export default TrafficMonitoringPanel;
